package com.blackbear.leetcode;

public class P10 {
}

class Solution10 {

    /**
     * 首先学到一个点，在正则表达式中 * 号表述的是它前面的那个字符可以出现0到n次。0次表达的意思本质上是消除
     * @param s
     * @param p
     * @return
     */
    public boolean isMatch(String s, String p) {
        if ((s == null || s.equals("")) || (p == null || p.equals(""))) {
            return false;
        }

        return false;
    }
}
